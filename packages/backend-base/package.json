{
  "name": "@delego/backend-base",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "exports": { ".": "./src/index.ts" },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "tsc",
    "test": "bun test || true",
    "clean": "rm -rf dist .turbo"
  },
  "dependencies": {
    "@delego/types": "workspace:*"
  },
  "devDependencies": { "typescript": "^5.5.0" }
}
