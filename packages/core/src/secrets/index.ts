// TODO(M3): env-passthrough and OS keyring access (macOS Keychain / Windows Cred Manager / libsecret).
// Likely candidates: `keytar` (works with Bun via napi), or platform-specific subprocesses.
export {};
