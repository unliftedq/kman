{
  "name": "delego",
  "private": true,
  "version": "0.0.0",
  "description": "Multi-agent orchestration engine — monorepo root",
  "type": "module",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "typecheck": "turbo run typecheck",
    "test": "turbo run test",
    "clean": "turbo run clean && rm -rf node_modules .turbo",
    "delego": "bun apps/cli/src/main.ts"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/node": "^25.9.1",
    "turbo": "^2.3.0",
    "typescript": "^5.6.0"
  },
  "packageManager": "bun@1.3.14",
  "engines": {
    "bun": ">=1.2.0"
  }
}
